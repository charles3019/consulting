# Server-only values. Set real values in your hosting environment.
# Generate AUTH_SECRET: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
AUTH_SECRET=5e72eda3a06172aff17529ea52843605037aace134398706e30aae414ade31d3
ADMIN_USERNAME=contact@connectforge.co.uk
ADMIN_PASSWORD=@30Matthew633

# Recommended for production and required for multiple application processes.
MYSQL_HOST=127.0.0.1
MYSQL_PORT=3306
MYSQL_USER=appuser
MYSQL_PASSWORD=StrongPassword123!
MYSQL_DATABASE=Connectforge

# Alternative for a SINGLE Node.js process on a persistent disk (not serverless).
# Create this directory and copy your db_fallback.json into it before launch.
DATA_DIR=


